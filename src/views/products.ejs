<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1>Product</h1>


    <ul>
        <% data.forEach(element=> { %>
            <% if (element.price>= 40000 && element.isActive) { %>
                <li>
                    <a href="/product/<%= element.id %>">
                        <%= element.name %> - <%= element.price %>
                    </a>
                </li>
                <% } %>

                    <% }) %>
    </ul>

</body>

</html>